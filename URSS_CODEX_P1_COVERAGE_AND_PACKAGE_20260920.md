# 本地 Codex 执行任务：核对 P1 完整覆盖并打包修复验收材料

请实际执行本任务，持续完成覆盖核对、必要补跑、验收、报告和打包，不要只返回计划。本任务授权修改必要的 P1 实现、补充验证和创建交付文件；不执行 commit、push、merge 或 Release。

## 1. 工作目录和现有材料

项目根目录：

```text
C:\Users\rocyz\Desktop\量子算法组合优化tsp\协作\URSS_REPAIR_20260920_154042_4937f93c\URSS_REPAIR_WORKSPACE_20260920\project
```

先确认当前工作目录和实际文件。主要输入：

- `FIX_REPORT.md`：本地最新修复报告；如存在多个版本，按内容和实际证据识别，报告选择依据。
- `../review/URSS_Code_ReReview_20260919_v2.md`：R1–R12 验收要求，只读。
- `URSS_CODEX_TASK_MULTISEED.md`：原实验任务，重点读取第 5 节 P0/P1。
- `results/multiseed_20260918T163717Z/`：原冻结 run、候选和样本清单、正式资源记录、历史 P1。
- `corrections/review_v2_20260920/`：本轮修复产物、108 条重放和另 108 条新预检证据。
- `README_REPRODUCE_REPAIRS.md`：修复后实际运行入口、环境与依赖说明。

本次远程审阅者只收到修复报告，尚未收到修复后的源码和验收证据。不要把报告里的 PASS 当作本轮已验证结果。

所有修改和新增文件限定在当前 `project` 内。不要因 Git 向上搜索而修改、暂存或提交上层“协作”仓库。允许只读使用现有匹配环境；依赖安装使用当前项目中的独立环境，避免改动共享环境。

## 2. 首先判断 108 条预检代表什么

原方案要求：

1. 两个 family 分别按原声明哈希排序选第一个实例。
2. 对两个实例覆盖三种拓扑、两种 synthesis，以及各 setting 冻结候选库的全部候选。
3. seed 1729 做两次独立编译；seed 2718 做一次编译。

历史 P1 报告记录 2,052 行；最新报告记录 108 条有限代表预检。先读实际 manifest、逐任务输出及代码，确认 108 是编译记录数、批次数、不同候选数，还是仅语义检查数。不要仅凭数字较小就判失败，也不要把代表样本当作全部候选。

从原冻结 `instance_manifest.csv` 的选择规则和 `candidate_manifest.csv` 独立构造预期任务集合。不得使用可能已缩小的预检任务列表作为唯一预期来源。

比较键至少包含：

```text
instance_id, candidate_id, topology, synthesis, seed, repetition
```

`1729/repeat_a`、`1729/repeat_b`、`2718/single` 必须分开。重试 attempt 与独立重复实验 repetition 也必须区分；不能复制同一结果或同一缓存行凑成两次独立重复。

预期总数按实际冻结候选计算：两个指定实例各 setting 的候选数之和 × 3。2,052 只作历史参考，不硬编码为 PASS 条件；若重新计算数量不同，查明原因并列出候选差异。

生成 `P1_COVERAGE_BEFORE.json` 和 `p1_coverage_before.csv`，记录每个实例、拓扑、synthesis、seed/repetition 的预期、实际、缺失、重复、额外和失败数量，列明自然键差集。核对实例选择规则、候选输入哈希、配置身份、编译实现身份和实际环境。

## 3. 如覆盖不足，补齐原方案 P1

如果 108 确实只是代表子集，恢复全部候选的 P1 默认行为，并保留代表预检为明确命名的 smoke/representative 模式。该模式不能满足正式全候选 P1 gate。

如果实际覆盖已经完整，只是报告的计数口径不清，修正说明并提供逐任务映射，不重复运行有效工作。

补跑规则：

- 使用新建、带唯一时间戳的目录，例如 `corrections/review_v2_20260920/p1_full_<UTC>/`；不覆盖旧 `preflight_new/`、108 条重放、原冻结 run 或历史失败记录。
- 使用当前已修复的正式 worker 和真实进程调度路径，避免另写一个绕开正式验证的简化 runner。
- 输入、配置和编译实现身份匹配且通过严格校验的已有记录可以复用；明确记录来源。两次独立 1729 重复不能共用同一观察记录。
- 如升级预检范围需要改变冻结配置，创建新版本与派生关系，保留旧配置。不得直接修改旧配置后沿用旧哈希。
- 普通 compile_error、timeout、缺结果或非法资源不得计为通过；可信 width_exceeded 单独记录依据和覆盖状态。
- 预先固定样本和候选，不按结果好坏调整覆盖范围。
- 不启动 101,460 条正式编译全量重跑，也不运行无关 QAOA 优化。

全候选 P1 应核对完整任务集合、资源合法性、实际 callback 记录、1729 两次资源和预算分类一致性，以及 2718 结果完整性。语义检查可以保留预先声明的有限样本和角度，但报告必须分别列出“全部候选编译预检范围”与“有限语义验证范围”，不能将后者称为全部电路证明。

新增或调整回归，至少证明：缺一个候选、缺一个 setting、缺第二次 1729 重复、重复 task、代表子集冒充完整 P1 时均不能通过；完整有效预检可以通过。独立覆盖验证不得仅调用生产 P1 任务生成器来证明自身正确。

完成后输出 `P1_COVERAGE_AFTER.json`、`p1_coverage_after.csv`、`P1_FULL_REPORT.json`、逐任务清单与结果、完整命令及日志、环境版本和来源哈希。若存在阻塞，记录准确命令、错误和已完成任务；允许生成标明 BLOCKED 的材料包，不能把缺失记录补成成功。

## 4. 复核修复验收证据

使用修复后的实际入口检查以下证据，并保存日志和退出码：

1. 仓库相关回归、完整测试及 golden 测试。只有代码或环境变化影响已有验收时才重跑必要测试；有效且身份匹配的原测试记录可复用，并标为复用。
2. 最终 `final_analysis/AUDIT.json` 和独立修正验证结果。确认覆盖完整、无重复、状态与数值合法，且最终报告引用的是最终验收目录。
3. 原始 run 及冻结配置保全检查，区分允许修改的源码与不应改动的历史文件。
4. `79,069 / 149,439` 从实际数据推导，确认没有硬编码结论。若数据或后处理代码未改变，可引用本轮修复中已有的独立完整复算；若改变则重新验证。
5. 当前 108 条正式 worker 重放的任务清单、预先选择规则、实际 pass 观测、有限语义检查、Q/G/D/M/J 对比和配置/实现/环境身份。
6. 环境锁文件来自实际运行解释器，复现命令不得依赖打包目录外不可访问的绝对源码路径。现有环境运行与全新环境安装验收分开报告。

论文 PDF 编译、最终稿采纳、全新环境安装、heavy-hex 53/54 等未解决项继续如实标为 BLOCKED/未验收。本次不扩大到解决全部投稿事项，不将它们的缺口掩盖为 R1–R12 无条件通过。

## 5. 打包可独立检查的交付材料

在项目内部新建 `delivery/repair_acceptance_<UTC>/`。输出两个 ZIP，以及外部 `SHA256SUMS.txt`。ZIP 使用相对路径，保留项目内目录结构，支持 ZIP64。

### A. `URSS_REPAIR_SOURCE_<UTC>.zip`

包含修复后的完整相关源码和新增文件，而不只是三个被修改的旧脚本：

- `scripts/` 中执行、编译、校验、进程调度、独立核验、重放、论文产物映射等全部必要模块。
- `urss_pipeline/`、`golden_example/`、`tests/`、必要配置、schema、测试 fixture，以及运行所需根目录脚本。
- 实际使用的依赖锁文件、修复复现说明、原任务、R1–R12 修复报告和本次 P1 复核报告。
- 与旧快照的 `CHANGE_MANIFEST.csv`：新增、修改、删除的相对路径、旧/新 SHA-256 和用途；以原 `FILE_MANIFEST.csv` 为基准，不依赖上层 Git 状态。
- 必要的冻结输入和精简测试数据；大规模原始结果通过证据包或基础归档索引引用。

### B. `URSS_REPAIR_ACCEPTANCE_EVIDENCE_<UTC>.zip`

包含：

- `corrections/review_v2_20260920/` 下最终验收证据、最终表格/图、独立核验输出、完整 YAML/JSON 配置、产物映射。
- P1 覆盖前后报告、全候选任务/结果、callback 与语义记录、所有用于认定 PASS 的日志和环境信息。
- 原 108 条重放和代表预检的报告与任务记录；历史失败记录保留并区分最终验收版本。
- 必要的 P1 冻结样本/候选/输入身份资料，使审阅者能够重算覆盖，而非只读汇总数量。
- 每个 R1–R12 状态到源码、测试、日志、实际产物的相对路径索引 `EVIDENCE_INDEX.json`。

两包都应包含文件级 `FILE_MANIFEST.csv`（相对路径、字节数、SHA-256）；清单自身不纳入自身哈希。外部 `SHA256SUMS.txt` 记录两个最终 ZIP 的哈希。

排除虚拟环境、缓存、Git 数据库、临时 staging、重复 ZIP 和本次打包输出本身。不要递归把 `delivery/repair_acceptance_<UTC>/` 再打进包内。

对超过 50 MiB 的文件先列出用途，不因体积大静默删除必要证据。若某文件只是可重新生成的大预算明细或重复的原始 run，可以改为记录源版本、大小、SHA-256、生成命令和依赖后省略；在 `OMITTED_LARGE_FILES.csv` 中明确列出。必要且不可重建的证据应保留；如需拆分，使用独立 ZIP 并提供跨包索引。

若依赖原基础包，写入 `BASE_ARCHIVE_REQUIREMENTS.json`，说明依赖的是：

```text
delivery.zip
SHA-256: 8b22f48da0a2e49669400ed0628cbca0f943997a95036b8385549d5779125e27

其内层 URSS_ALL_PROJECT_CODE_20260918T190812Z.zip
SHA-256: 54e5629261b6bceb1b8922f7a33aa9f75f87fae5af80f7ccb4ba116349a32014
```

明确哪一步需要基础包，不能将依赖基础包的增量交付描述为完全独立的全量项目。

## 6. 对实际生成的 ZIP 验收

在新的临时目录重新解压最终 ZIP，而不是只检查打包源目录。逐文件验证清单哈希、路径安全性与 ZIP 完整性；核对修复源码、新增模块、测试、依赖文件、最终报告和证据索引均可找到。

按交付 README 的目录结构，执行轻量入口和独立 P1 覆盖检查，确认相对路径可用。对于声明依赖基础包的检查，按说明放入基础包数据后执行。无需为了打包验收再做一次全部编译。

产生 `PACKAGE_VALIDATION.json`，将 ZIP 完整性、文件哈希、依赖解析、入口验证分别记录。报告中所有“文件已存在”“检查通过”均须有实际证据。

## 7. 最终答复格式

完成后简洁汇报：

1. 108 的真实计数含义；原 P1 覆盖是否满足。
2. 按冻结清单推导的预期任务数、实际有效数、缺失/重复/额外/失败数；本轮新增运行数和复用数。
3. 必要源码修正和测试结果，真实剩余 BLOCKED 项。
4. 两个 ZIP（如拆分则列全部）、外部哈希和包装验收报告的绝对路径及大小。
5. 明确本次未执行 commit、push、Release，未重跑正式全量编译。

最终报告文件使用 `P1_COVERAGE_AND_PACKAGING_REPORT.md`；保留原 `FIX_REPORT.md`，如需更新其中状态，保留修改前副本并记录变化。
