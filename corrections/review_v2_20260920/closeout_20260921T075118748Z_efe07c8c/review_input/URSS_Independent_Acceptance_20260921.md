# URSS 修复包独立验收 · 2026-09-21

**结论：R1–R11 在本轮限定范围内通过；R12 部分通过。完整 P1 的覆盖缺口已经关闭，但当前交付仍有两项需要补正，不能写成“R1–R12 全部无条件 PASS”。**

本轮检查的是实际上传的源码 ZIP 和证据 ZIP，不仅是其中的 PASS 报告。可以将当前成果作为修复分支提交审查；如要将它标为完整验收交付，应先完成下述两项补正。论文 PDF、最终稿采纳和全新环境安装仍未验收。本次没有修改交付源码，也没有执行 Git 提交、推送、合并或发布。

**发现 1：完整仓库测试所需的冻结数据未进入包，影响独立复现。**

`scripts/package_repair_acceptance.py` 的源码目录清单没有包含 `fibre_selector_v2/` 或 `tmp_fibre_selector_v2_full_output/`。`tests/test_fibre_rerun.py::FibreRerunBundleTests.setUpClass()` 在两者均不存在时跳过整组测试。仅用两个 ZIP 组合的目录确实缺少它们，因此该组 5 个测试没有执行。

随包 Windows 日志中的 **193/193 PASS** 来自打包前的完整工作区；命令记录的 cwd 也是原 project，而非重新解压后的 assembled。它可以作为原工作区测试记录，但不能证明这两个 ZIP 能重现全部 193 项。`BASE_ARCHIVE_REQUIREMENTS.json` 中“repository tests with installed dependencies”自包含的声明因此过宽。现有 `validate_acceptance_package.py` 只执行帮助入口、P1 覆盖核对和 gate/依赖读取，没有运行完整测试，未能发现这一缺口。

补正要求：纳入匹配版本的冻结 bundle 及其包清单依赖，在全新解压目录、已有完整依赖解释器中运行全部 193 项并确认没有这组跳过；更新 ZIP 清单和外置验证报告。如果决定不纳入，则必须缩小“自包含”和完整测试复现的声明，并说明需要哪份历史 bundle。对本次声称的完整测试交付，优先采用补齐数据的做法。

**发现 2：论文映射中的两个源码哈希尚未更新到本次交付。**

`corrections/review_v2_20260920/PAPER_ARTIFACT_MAP.json` 中 27 个文件绑定有 25 个匹配；以下两个仍记录上一修复阶段的源码。它们与包内 `p1_coverage_20260920T232002Z/prior_source/scripts/` 的对应文件匹配，属于历史映射未更新，未发现论文表图被篡改。

| 文件 | 映射记录 SHA-256 | 当前源码 SHA-256 |
|---|---|---|
| `scripts/multiseed_experiment.py` | `e3963bb6239133d678468208fd3ff9a1cd62511af67fc30e90cc03cd4f1fbed3` | `d728399a8735c04da583172ff276a32d0dafcc4ecc6bde70cc72a8a4ba8d690a` |
| `scripts/multiseed_scheduler.py` | `18f65798c4e380e6d2d181afe29bc4e46f145bc8b799273c90286ce38c478d5a` | `696df564b99adf47fda2e71b7e5f78b879a5f44592045acc3909ec02ff45fb6b` |

补正要求：保留旧映射作为历史记录，新增明确版本的映射，绑定当前源码、`multiseed_p1.py`、完整 P1 配置与产物，并在交付入口指向最新映射。新映射应核验其每个文件哈希，而不是只检查路径存在。

数值分析相关的 7 个函数在完整 P1 修改前后 AST 相同，共享资源校验和独立数值核验脚本也保持字节一致；本轮另外重新进行了数值核验。因此，该映射缺口不推翻已验证的保留率和修正表图。

**实际完成的独立检查**

| 检查 | 本轮结果 |
|---|---|
| 两个 ZIP 的 SHA-256、大小、CRC、路径安全 | 全部匹配；无危险路径、重复条目或符号链接 |
| 包内逐文件清单 | 源码 975 项、证据 3,904 项，共 4,879 项，大小和哈希零差异 |
| 两包组合 | 复制 4,877 个文件，按哈希恢复 7,703 个重复路径；核心验收未借用基础包 |
| 完整 P1 独立覆盖核对 | 2,052/2,052；missing=0、duplicate=0、extra=0、failed=0 |
| 正式 P1 gate | 解压目录中的真实 `require_full_gate()` 通过，编译实现身份与交付源码匹配 |
| P1 原始记录与汇总 | 36 个 attempt JSON 与 CSV 的 2,052 条记录逐项相等；2,052 个独立观察 ID，2,052 个不同 PID/启动时间组合 |
| P1 实际 callback | 2,052 条都有实际 pass 与 SABRE 参数记录，参数与声明一致 |
| 同 seed 重复 | 两次 1729 的资源一致；12 个 setting 的预算分类零差异 |
| P1 有限语义记录 | 108 条、216 个角度误差值，最大误差 `1.4637612725888058e-16`，低于 `1e-9` |
| 正式保存资源 | 101,460 条自然键完整唯一，全部 compiled；raw、汇总、digest/trace 关联一致 |
| 历史资源保持 | 原 run 与 final_analysis 的 `compilation_rows.csv` SHA-256 相同 |
| 分析状态绑定 | 8 个已绑定分析文件全部匹配；配置和编译资源来源一致 |
| 独立数值复算 | 1,800 个分类数组、1,800 行 selector、稳定性标签、逐 seed 图表 CSV 和完整 YAML 均通过，differences=[] |
| 修复相关回归 | 本轮执行 27/27 PASS，包含真实可终止进程超时回归 |
| Golden | 本轮执行 10/10 PASS |
| 旧有限重放证据 | 108 条保存记录通过重新核对，Q/G/D/M/J 对历史零差异；最大有限语义误差 `2.2594065722754323e-16` |
| R1–R12 证据路径索引 | 33 个不同源码、测试、日志、产物路径全部可解析 |

这里的 684 指两个选定实例在 3 个拓扑 × 2 种 synthesis 下的候选配置，包含 setting 维度：`max3sat_003` 为 6×61=366，`pair_star_isolated_025` 为 6×53=318。分别执行 `1729/repeat_a`、`1729/repeat_b`、`2718/single`，合计 684×3=2,052。不能将原来的 108 条代表子集视为该完整覆盖。

**数值结论**

本轮重新执行不调用生产分类/selector 函数的核验脚本，得到 **79,069 / 149,439 = 52.910552131639%**。稳定性分类为 stable=49、partial=171、disappeared=35、empty_baseline=105。五个 seed 的非空 TRUE setting 数依次为 255、255、262、254、256，分母均为 360。核验没有用旧报告数字回填结果。

**逐项判断**

| 项目 | 本轮判断 | 依据与范围 |
|---|---|---|
| R1 审核、缓存恢复 | 通过 | 校验接入实际恢复、写入和审计路径；污染缓存与任务覆盖反例通过；101,460 条实际保存记录通过 |
| R2 P1 | 通过 | 默认完整候选；代表模式不能满足 gate；独立验证 2,052 条与两次 1729；缺候选、缺 setting、缺重复、重复观察等回归通过 |
| R3 配置和实现身份 | 通过 | 配置内容、输入、候选清单和编译身份核验；篡改回归与真实 gate 通过；完整 YAML 与 JSON 相等 |
| R4 稳定性 | 通过 | 区分任一种子丢失、所有其他种子丢失；stable 与空基准定义正确；独立完整向量复算通过 |
| R5 基准 selector | 通过 | 固定基准 J winner、明确预算分母、逐 seed 同候选覆盖；1,800 行独立复算及异常反例通过 |
| R6 逐 seed 图 | 通过 | 对应 seed 的明细驱动图表；CSV 独立核对与回归通过 |
| R7 容量排除 | 通过 | 仅有冻结宽度证据时认可排除；伪造/缺少证据的回归通过；本批真实资源没有该状态 |
| R8 非法资源 | 通过 | 入口拒绝 NaN、Inf、负值、非整数及非法宽度；实际保存资源通过 |
| R9 超时 | 通过 | 正式与 P1 接入可终止子进程调度；从启动前计时；真实挂起进程回归通过 |
| R10 实际 trace | 通过，限已核对执行记录 | callback 已接入正式 worker；本次完整 P1 的 2,052 条实际观测通过；不将旧 declared-only trace 升格为实际观测 |
| R11 正式语义入口 | 通过，有限验证 | 正式 worker 的固定角度语义检查已接入；108 条 P1 语义记录和108 条旧重放记录通过；不等同于所有电路、参数和输入态的完整证明 |
| R12 环境、产物、论文映射 | 部分通过 | 锁文件、分析入口和修正图表齐备；仍需补齐测试 bundle、当前版本源码映射；干净安装、PDF 和最终稿采纳未验收 |

**测试与证据边界**

本次实际环境为 Python 3.12.14、NumPy 2.3.5、SciPy 1.17.0、PyYAML 6.0.3，缺少 Qiskit、Qiskit Aer、jsonschema、psutil。仓库测试实际结果是 `Ran 188 tests`、`FAILED (errors=15, skipped=2)`：172 项通过，15 项因这些缺失依赖报错，1 项因 Qiskit 缺失跳过；另有 1 次 class 级跳过，使冻结 bundle 对应的 5 个测试没有计入 188。后者是交付数据缺失，与运行环境缺少依赖是两件事。

随包的 Windows 193/193 日志和命令记录已读取，未据此声称本环境也重现了 193/193。没有安装或冒用与历史不一致的量子编译环境。本轮新 Qiskit 编译次数为 0；callback、有限语义和旧重放结果是对上传记录、实现及绑定关系的验收。其独立覆盖、数值复算、回归和 gate 检查则在本环境实际执行。

包中主动省略的巨大预算明细在 `OMITTED_LARGE_FILES.csv` 中有哈希和重建方法；用于本次数值核验的资源向量、紧凑数组和统计表齐备，因此不把该已说明的省略列为新增阻断。本轮未重新生成约 573 MB 的展开预算表，未再次逐字节复查整个历史工作区的 4,216 个文件，也未为 heavy-hex 53/54 背书。

**交付身份与复跑方法**

| 输入 | 字节数 | SHA-256 |
|---|---:|---|
| `URSS_REPAIR_SOURCE_20260920T232002Z.zip` | 5,221,267 | `790647bd9729479bc60f76d883531e7b5b547a85275825387e79f6c34b8229d1` |
| `URSS_REPAIR_ACCEPTANCE_EVIDENCE_20260920T232002Z.zip` | 46,533,236 | `23921993b38b2c7ab392147e23382d7d1db7b739e89e228b15bc54c094598409` |

配套 `URSS_Independent_Acceptance_Evidence_20260921.zip` 保存本轮核验脚本、JSON、日志、命令和逐文件 SHA-256。按用户交付的 `DELIVERY_README.md` 组合原两包后，在该证据包的目录使用 `acceptance_checks.py --project <assembled绝对路径> --output <新结果文件>` 可复跑读取检查；先将完整测试输出保存为同目录的 `repository_tests.log`，脚本才能同时统计测试结果。脚本不会执行量子编译或改写交付目录。覆盖与数值复算的独立命令见配套 `REVIEW_COMMANDS.md`。

下一次验收只需针对两项新增交付问题补证：补齐冻结 bundle 后的解压目录完整测试日志，以及指向当前源码的新版映射及逐项哈希校验。无需为这两项问题重跑 101,460 次量子编译；若量子编译实现或配置再次改变，应按已有协议重新取得匹配的 P1 证据。
