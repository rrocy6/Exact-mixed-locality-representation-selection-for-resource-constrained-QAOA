\paragraph{Correction provenance.}
The frozen plan is \texttt{warmstart\_coordinate\_correction\_plan\_v1};
the execution is \texttt{exec\_20260912\_v2}.
The local source snapshot is \texttt{1301a48c88d2}; its full hash, source
manifest, environment snapshot, task attempts, and all 4,800 row identities
are recorded in the accompanying audit. This snapshot is not an external
GitHub release. The Linux/Python 3.12.14, NumPy 2.3.5, SciPy 1.17.0
environment differs from the Windows archive, so all positive-depth cold
tasks were rerun uniformly. Changes from historical optimization results
cannot be attributed solely to the mixer. Archived LP moments were
restored and hash-checked without resolving the LP. Historical results
were known when the plan was frozen; this is an exploratory correction
study, not an outcome-blind confirmatory preregistration.
Recomputing the archived original E4 rows with the same instance-level
Student-t method also yields eight primary pair-minus-cold intervals
containing zero. The change in claim strength must therefore not be
attributed exclusively to the mixer correction.
